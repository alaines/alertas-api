# Copia este archivo a .env y ajusta las credenciales reales
DATABASE_URL="postgresql://alertas:TU_PASSWORD@localhost:5432/monitoreo_trafico?schema=public"

# Puerto HTTP (por defecto 80, requiere sudo en Linux)
PORT=80

# JWT Secret para autenticación (cambiar en producción)
# Genera uno seguro con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="tu-secret-jwt-super-seguro-cambialo-en-produccion"
