# Copia este archivo a .env y ajusta las credenciales reales
DATABASE_URL="postgresql://alertas:TU_PASSWORD@localhost:5432/monitoreo_trafico?schema=public"

# Puerto HTTP (por defecto 80)
PORT=80

# API Keys permitidas (separadas por comas)
# Genera tus propias keys con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_KEYS="tu-api-key-1,tu-api-key-2,tu-api-key-3"
